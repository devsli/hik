version: "2.1"

services:
  www:
    image: nginx
    environment:
      VIRTUAL_HOST: "kih.sli.gonzoweb.ru"
    volumes:
      - "feed:/usr/share/nginx/html"

  updater:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - "feed:/app/out"

volumes:
  feed:
