version: "2.1"

services:
  www:
    image: nginx
    volumes:
      - "feed:/usr/share/nginx/html"

  updater:
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      VIRTUAL_HOST: "kih.suprun.pw"
    volumes:
      - "feed:/app/out"

volumes:
  feed:
